// Single source of truth for the campaign page's URLs.
// The slug doubles as the page's target keyword.
export const LANDING_PATH = "/homeopathy-treatment-in-delhi";
export const THANK_YOU_PATH = `${LANDING_PATH}/thank-you`;

// Old slug, kept so existing ad links / bookmarks keep working via a redirect.
export const LEGACY_LANDING_PATH = "/google-landing";
export const LEGACY_THANK_YOU_PATH = `${LEGACY_LANDING_PATH}/thank-you`;
